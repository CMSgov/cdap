services:
  - name: "tftesting-service"
    lifecycle_enabled: false
    tag_rules: []
    untagged_expiry_days: 2
  - name: "mtls-sidecar"
    tag_rules:
      - tag_prefix: "." # matches all tagged images
        keep_count: 5
    untagged_expiry_days: 20
