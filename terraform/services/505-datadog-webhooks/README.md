# Datadog Webhooks

## Slack 

This module will create a slack webhook for SSM parameters storing a URL at the path: `/cdap/prod/datadog/slack_webhook_urls/`
This webhook can be used to notify that channel. The Slack webhook can be generated with a slack workflow.
