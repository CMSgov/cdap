image:
  enabled: true
  test_command: >-
    go test ./...
    -v
    -coverprofile=/app/coverage/coverage.out &&
    sed -i 's|mtls-sidecar/||g' /app/coverage/coverage.out &&
    go tool cover
    -xml=/app/coverage/coverage.out
    -o /app/coverage/coverage.xml
  docker_target: tester