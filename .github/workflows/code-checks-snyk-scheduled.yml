name: code-checks (snyk code scheduled)
on:
  schedule:
    - cron: '0 0 */2 * *'
  workflow_dispatch:

jobs:
  scheduled-code-checks:
    name: 'Scheduled Code Checks for CDAP'
    uses: CMSGov/CDAP/.github/workflows/code-checks-snyk.yml@main
    with:
      app: cdap
      ref: main
    secrets: inherit
